######################################################################################
#AUTOGENERATED HYPR CONFIG.
#PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
#OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

# Please note not all available settings / options are set here.
# For a full list, see the wiki


# exec-once = hyprpm reload -n && hyprctl reload
#plugin = /usr/lib/libhycov.so

$scriptsDir = $HOME/.local/bin
$hyprScriptsDir = $HOME/.config/hypr/scripts

# monitor = DP-1, 2560x1080@144, 0x0, 1
# monitor = ,preferred,auto,auto

# workspace=1,monitor:HDMI-A-1
# workspace=2,monitor:HDMI-A-2
# workspace=3,monitor:HDMI-A-2
# workspace=4,monitor:HDMI-A-2
# workspace=5,monitor:HDMI-A-1
# workspace=8,monitor:HDMI-A-1
# workspace=9,monitor:HDMI-A-2
# workspace=10,monitor:HDMI-A-1

exec-once = pamixer --set-volume 40
exec-once = hyprctl dispatch workspace 2
exec-once = [workspace 1 silent] firefox
exec-once = [workspace 2 silent] alacritty
# exec-once = kanshi
# exec-once = [workspace 5 silent] spotify
exec-once = [workspace special silent] firefox --new-instance -P private
# exec-once = [workspace special silent] alacritty
#exec-once = [workspace 8 silent] alacritty -e cava
#exec-once = [workspace 9 silent] alacritty -e cava

exec-once = $hyprScriptsDir/resetxdgportal.sh # reset XDPH for screenshare
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # for XDPH
exec-once = dbus-update-activation-environment --systemd --all # for XDPH
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # for XDPH
exec-once = $scriptsDir/Polkit.sh # authentication dialogue for GUI apps
exec-once = waybar & # launch the system panel
exec-once = blueman-applet # systray app for BT
exec-once = swaync & # control centre
exec-once = nm-applet --indicator # systray app for Network/Wifi
exec-once = dunst # start notification demon
exec-once = wl-clipboard-history -t
exec-once = wl-paste --type text --watch cliphist store # clipboard store text data
exec-once = wl-paste --type image --watch cliphist store # clipboard store image data
exec-once = rm "$HOME/.cache/cliphist/db" # Clear clipboard
exec-once = swww query || swww init # Wallpaper daemon
exec-once = $hyprScriptsDir/batterynotify.sh # battery notification

env = CLUTTER_BACKEND,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = QT_QPA_PLATFORM,wayland
#env = QT_STYLE_OVERRIDE,kvantum
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = MOZ_ENABLE_WAYLAND,1

input {
    kb_layout = gb,gb,de,fr 
    kb_variant = extd,dvorak,,
    repeat_delay = 300
    repeat_rate = 50
    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    force_no_accel = 1
}

binds {
  workspace_back_and_forth=1
  #allow_workspace_cycles=1
  #pass_mouse_when_bound=0
}

device:epic mouse V1 {
    sensitivity = -0.5
}

gestures {
    workspace_swipe = true
    workspace_swipe_fingers = 3
}

general {
    gaps_in = 3
    gaps_out = 8
    border_size = 2
    col.active_border = rgba(ca9ee6ff) rgba(f2d5cfff) 45deg
    col.inactive_border = rgba(b4befecc) rgba(6c7086cc) 45deg
    layout = master # dwindle or master
    resize_on_border = true
}

group {
    col.border_active = rgba(ca9ee6ff) rgba(f2d5cfff) 45deg
    col.border_inactive = rgba(b4befecc) rgba(6c7086cc) 45deg
    col.border_locked_active = rgba(ca9ee6ff) rgba(f2d5cfff) 45deg
    col.border_locked_inactive = rgba(b4befecc) rgba(6c7086cc) 45deg
}

decoration {
    rounding = 10
    drop_shadow = false

    blur {
        enabled = yes
        size = 6
        passes = 3
        new_optimizations = on
        ignore_opacity = on
        xray = false
    }
}

dwindle {
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    new_is_master = true
    new_on_top=1
    mfact = 0.5
}

misc {
    disable_hyprland_logo = true
    mouse_move_focuses_monitor = true
    no_direct_scanout = true #for fullscreen games
    # vrr = 1
}

source = $HOME/.config/hypr/animations.conf
source = $HOME/.config/hypr/keybindings.conf
source = $HOME/.config/hypr/windowrules.conf
source = $HOME/.config/hypr/themes/common.conf # shared theme settings
source = $HOME/.config/hypr/themes/colors.conf # wallbash color override
source = $HOME/.config/hypr/monitors.conf # initially empty, to be configured by user and remains static
source = $HOME/.config/hypr/workspaces.conf # initially empty, to be configured by user and remains static
source = $HOME/.config/hypr/userprefs.conf # initially empty, to be configured by user and remains static
source = $HOME/.config/hypr/nvidia.conf # auto sourced vars for nvidia
